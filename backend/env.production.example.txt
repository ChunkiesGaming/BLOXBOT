# === PRODUCTION .env (copy to .env on server and fill placeholders) ===

# Server
PORT=4000
NODE_ENV=production

# Database (use your real Postgres credentials)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE_NAME
DB_HOST=localhost
DB_PORT=5432
DB_NAME=questlayer
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Security (generate random strings; never commit these)
JWT_SECRET=generate-a-long-random-string-here
ROBLOX_API_KEY=your-roblox-server-api-key-from-roblox
API_SECRET=your-backend-api-secret-for-roblox-game

# Roblox OAuth (from Roblox Create → Your App → OAuth2)
ROBLOX_CLIENT_ID=your-roblox-client-id
ROBLOX_CLIENT_SECRET=your-roblox-client-secret
ROBLOX_REDIRECT_URI=https://api.chunkiesgaming.com/api/v1/auth/roblox/callback

# CORS = your frontend URL (where users open the web app)
CORS_ORIGIN=https://bloxbot.chunkiesgaming.com


  GNU nano 7.2                                              .env

