# === LOCAL DEV .env for web (copy to .env.local) ===

# Backend API URL (local backend)
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# Optional: Roblox OAuth (login usually goes through backend callback)
NEXT_PUBLIC_ROBLOX_CLIENT_ID=your-roblox-client-id
NEXT_PUBLIC_ROBLOX_REDIRECT_URI=http://localhost:4000/api/v1/auth/roblox/callback
