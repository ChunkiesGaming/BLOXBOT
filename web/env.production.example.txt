# === PRODUCTION .env for web (copy to .env.local on server / Vercel) ===
# Next.js reads .env.local in production; Vercel uses "Environment Variables" in dashboard.

# Backend API (must match your deployed API URL)
NEXT_PUBLIC_API_URL=https://api.chunkiesgaming.com/api/v1

# Optional: if your frontend uses Roblox OAuth directly (login uses backend URL by default)
NEXT_PUBLIC_ROBLOX_CLIENT_ID=your-roblox-client-id
NEXT_PUBLIC_ROBLOX_REDIRECT_URI=https://api.chunkiesgaming.com/api/v1/auth/roblox/callback
